<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/spellingLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/spelling_list_appbar_height"
        android:fitsSystemWindows="true"
        android:background="@color/primaryColor"
        android:theme="@style/ThemeOverlay.Material3.Dark.ActionBar">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:contentScrim="@color/primaryColor"
            app:titleEnabled="false">

            <View
                android:id="@+id/overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/primaryColor"
                app:layout_collapseMode="parallax"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="bottom|center_horizontal"
                android:paddingBottom="@dimen/spelling_list_header_padding_bottom"
                app:layout_collapseMode="parallax">

                <TextView
                    android:id="@+id/nameTextExpanded"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Spelling Progress"
                    android:textColor="@android:color/white"
                    android:fontFamily="@font/pixel"
                    android:textSize="@dimen/spelling_list_title_text_size"
                    android:textStyle="bold"
                    android:layout_marginBottom="@dimen/spelling_list_title_margin_bottom" />

                <TextView
                    android:id="@+id/studentIdProgress"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="S00000"
                    android:background="@drawable/rounded_button"
                    android:padding="@dimen/spelling_list_student_id_padding"
                    android:textColor="@android:color/black"
                    android:textSize="@dimen/spelling_list_student_id_text_size"/>
            </LinearLayout>

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin"
                app:title=""
                app:navigationIconTint="@android:color/white"
                app:navigationIcon="@drawable/back"/>
        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/achievementsRecycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:padding="@dimen/spelling_list_recycler_padding"
        android:overScrollMode="never"
        android:scrollbars="vertical"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
